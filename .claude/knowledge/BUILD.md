# BUILD

프로젝트 빌드 관련 정보입니다.

## 기본 명령어

```bash
# 빌드
(빌드 명령어)

# 릴리스 빌드
(릴리스 빌드 명령어)

# 클린 빌드
(클린 빌드 명령어)
```

## 빌드 환경

- 언어/런타임: (예: Node.js 18, Python 3.11, .NET 8)
- 패키지 매니저: (예: npm, pip, nuget)
- 빌드 도구: (예: webpack, make, msbuild)

## 의존성

(주요 의존성 목록)

## 환경 변수

| 변수 | 설명 | 기본값 |
|------|------|--------|
| (변수명) | (설명) | (기본값) |

## 빌드 문제 해결

| 문제 | 해결 방법 |
|------|-----------|
| (문제) | (해결 방법) |

## 참고

- 새로운 빌드 정보 발견 시 이 문서에 추가
- `/flush` 명령으로 CACHE에서 이동됨
